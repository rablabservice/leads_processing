function select_mris_to_process(data_dir, overwrite, verbose)
    % Return an array of MRIs to process by comparing raw to processed.
    % ------------------------------------------------------------------

end
